function multiply (num1, num2) {
  return num1 * num2;
}

console.log(multiply(5, 3) === 15); // logs true

/*
Notes:

The above solution can be also be drafted as an arrow function:

const multiply = (num1, num2) => num1 * num2;


*/
